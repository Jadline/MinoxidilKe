{
  "name": "minoxidilke",
  "private": true,
  "description": "MinoxidilKe – frontend built here, backend serves it on Render",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd ../Backend && npm ci",
    "start": "cd Backend && npm start",
    "install:all": "cd frontend && npm ci && cd ../Backend && npm ci"
  }
}
